[project]
name = "vision-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cv>=1.0.0",
    "mediapipe>=0.10.33",
    "opencv-python>=4.13.0.92",
]

[dependency-groups]
dev = [
    "ruff>=0.15.8",
]
